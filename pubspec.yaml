name: bdd_widget_test
description: A BDD-style widget testing library. Generates Flutter widget tests from *.feature files.
version: 1.8.2
repository: https://github.com/olexale/bdd_widget_test
issue_tracker: https://github.com/olexale/bdd_widget_test/issues

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  build: ^3.0.0
  build_config: ^1.1.2
  characters: ^1.4.0 # Locked at 1.4.0 by flutter_test
  collection: ^1.19.1
  dart_style: ^3.1.1
  diacritic: ^0.1.6
  file: ^7.0.1
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  meta: ^1.16.0 # Locked at 1.16.0 by flutter_test
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  build_runner: ^2.6.0
  build_test: ^3.3.0
  very_good_analysis: ^9.0.0
